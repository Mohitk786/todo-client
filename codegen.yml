overwrite: true
schema: "http://localhost:8000/graphql"  # Your GraphQL API
documents: "./**/*.{ts,tsx}"
generates:
  src/generated/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo"
